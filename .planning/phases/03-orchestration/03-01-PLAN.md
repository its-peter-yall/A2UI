<plan>
  <metadata>
    <phase>03</phase>
    <plan-number>01</plan-number>
    <title>Course Generation Orchestrator</title>
    <status>pending</status>
  </metadata>

  <objective>
    Implement the Scatter-Gather orchestration pattern for parallel agent execution,
    reducing course generation latency while maintaining narrative coherence.
  </objective>

  <context>
    <file path=".planning/ROADMAP.md" />
    <file path="server/agents/__init__.py" />
    <file path="server/database/learning_persistence.py" />
    <file path="Implementing Retrieval-Based Learning Feature.md" section="2.1: Scatter-Gather Pattern" />
  </context>

  <prerequisites>
    <prerequisite>Phase 02 completed (all agents implemented)</prerequisite>
  </prerequisites>

  <tasks>
    <task id="1">
      <title>Create Course Orchestrator</title>
      <type>implement</type>
      <files>
        <file>server/services/course_orchestrator.py</file>
        <file>server/services/__init__.py</file>
      </files>
      <action>
        Create CourseOrchestrator class implementing Scatter-Gather:
        
        Main method: generate_course(query: str, user_id: Optional[str]) -> LearningSessionWithNodes
        
        Flow:
        1. Call planner_agent.plan(query) - SERIAL (blocking)
        2. Create learning session in database
        3. Build context injection data for each topic:
           - prev_summary = topics[i-1].summary_for_context or "Start"
           - next_summary = topics[i+1].summary_for_context or "End"
        4. Scatter: Create coroutines for each topic
           - _generate_concept_unit(topic, prev_summary, next_summary, session_id)
        5. Gather: await asyncio.gather(*tasks, return_exceptions=True)
        6. Process results, handle partial failures
        7. Return complete session with nodes
        
        Helper method: _generate_concept_unit(topic, prev, next, session_id) -> Dict
        - Call generator_agent.generate_explanation()
        - Call quizzer_agent.generate_quiz()
        - Create concept_node in database
        - Return node data or error dict
        
        Singleton: course_orchestrator = CourseOrchestrator()
      </action>
      <verify>
        <command>python -c "from server.services.course_orchestrator import course_orchestrator; print('CourseOrchestrator imported')"</command>
      </verify>
      <done>false</done>
    </task>

    <task id="2">
      <title>Implement Partial Failure Handling</title>
      <type>implement</type>
      <files>
        <file>server/services/course_orchestrator.py</file>
      </files>
      <action>
        Add robust error handling for partial failures:
        
        In _generate_concept_unit:
        - Wrap in try/except
        - On failure, return SkeletonCard with:
          - status: "ERROR"
          - error_message: str
          - retry_available: True
        
        In _process_gather_results:
        - Iterate through results from asyncio.gather
        - If result is Exception, create SkeletonCard
        - If result is valid, use as-is
        - Log warnings for failures
        - Never fail the entire course for single card failure
        
        Add method: regenerate_node(node_id: str) -> ConceptNodeResponse
        - Re-run generation for a single failed node
        - Update database with new content
      </action>
      <verify>
        <command>Run unit test with mocked failure</command>
      </verify>
      <done>false</done>
    </task>

    <task id="3">
      <title>Add Performance Logging</title>
      <type>implement</type>
      <files>
        <file>server/services/course_orchestrator.py</file>
      </files>
      <action>
        Add timing and performance metrics:
        
        - Log planner execution time
        - Log total scatter-gather time
        - Log individual card generation times
        - Log success/failure counts
        - Calculate and log latency savings vs serial execution
        
        Use structured logging format:
        logger.info("Course generation complete", extra={
            "session_id": id,
            "planner_ms": planner_time,
            "parallel_ms": parallel_time,
            "total_ms": total_time,
            "cards_success": n,
            "cards_failed": m
        })
      </action>
      <verify>
        <command>Check logs show timing information</command>
      </verify>
      <done>false</done>
    </task>
  </tasks>

  <verification>
    <step>Import test: python -c "from server.services import course_orchestrator"</step>
    <step>Verify asyncio.gather used: grep -n "asyncio.gather" server/services/course_orchestrator.py</step>
    <step>Verify return_exceptions=True: grep -n "return_exceptions" server/services/course_orchestrator.py</step>
  </verification>

  <success-criteria>
    <criterion>Scatter-Gather pattern implemented</criterion>
    <criterion>Context injection passes prev/next summaries</criterion>
    <criterion>Partial failures don't crash course generation</criterion>
    <criterion>Performance logging shows timing data</criterion>
    <criterion>regenerate_node method available for retries</criterion>
  </success-criteria>

  <output>
    On completion, create 03-01-SUMMARY.md documenting:
    - Orchestration flow
    - Error handling approach
    - Performance considerations
    - Commit hash
  </output>
</plan>
