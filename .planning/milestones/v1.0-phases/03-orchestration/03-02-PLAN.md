<plan>
  <metadata>
    <phase>03</phase>
    <plan-number>02</plan-number>
    <title>Orchestrator Tests</title>
    <status>pending</status>
  </metadata>

  <objective>
    Create comprehensive tests for the course orchestrator including
    async behavior, partial failure handling, and context injection.
  </objective>

  <context>
    <file path=".planning/phases/03-orchestration/03-01-PLAN.md" />
    <file path="server/services/course_orchestrator.py" />
    <file path="server/tests/test_planner_agent.py" note="for mock patterns" />
  </context>

  <prerequisites>
    <prerequisite>Plan 03-01 completed (orchestrator implemented)</prerequisite>
  </prerequisites>

  <tasks>
    <task id="1">
      <title>Create Orchestrator Unit Tests</title>
      <type>test</type>
      <files>
        <file>server/tests/test_course_orchestrator.py</file>
      </files>
      <action>
        Create async unit tests with mocked agents:
        
        TestCourseOrchestrator:
        - test_generate_course_calls_planner: Verify planner called first
        - test_generate_course_creates_session: Verify DB session created
        - test_generate_course_parallel_execution: Verify asyncio.gather used
        - test_context_injection_first_topic: prev_summary is "Start"
        - test_context_injection_last_topic: next_summary is "End"
        - test_context_injection_middle_topic: Both prev and next populated
        - test_all_nodes_created_in_db: Verify all nodes persisted
        - test_first_node_unlocked: First node status is UNLOCKED
        - test_subsequent_nodes_locked: Other nodes status is LOCKED
        
        TestPartialFailureHandling:
        - test_single_failure_doesnt_crash: Course completes with one error
        - test_failed_node_has_error_status: SkeletonCard has error info
        - test_regenerate_node_replaces_content: Retry updates node
        - test_multiple_failures_logged: Warnings logged for failures
        
        TestPerformanceMetrics:
        - test_timing_logged: Verify timing info in logs
        - test_success_failure_counts: Verify counts logged
        
        Use:
        - unittest.IsolatedAsyncioTestCase for async tests
        - unittest.mock.AsyncMock for async agent mocks
        - Temporary database for isolation
      </action>
      <verify>
        <command>python -m unittest server.tests.test_course_orchestrator -v</command>
      </verify>
      <done>false</done>
    </task>

    <task id="2">
      <title>Create Integration Test</title>
      <type>test</type>
      <files>
        <file>server/tests/test_orchestrator_integration.py</file>
      </files>
      <action>
        Create integration test that exercises full flow (can be skipped in CI):
        
        TestOrchestratorIntegration:
        - test_full_course_generation: End-to-end with real agents
          - Requires Vertex AI credentials
          - Use @unittest.skipUnless(os.getenv("RUN_INTEGRATION_TESTS"))
          - Generate course for simple topic
          - Verify all nodes have content
          - Verify all nodes have quizzes
          - Verify quiz structure is valid
        
        Note: This test is for manual verification, not CI.
      </action>
      <verify>
        <command>RUN_INTEGRATION_TESTS=1 python -m unittest server.tests.test_orchestrator_integration -v</command>
      </verify>
      <done>false</done>
    </task>
  </tasks>

  <verification>
    <step>Run unit tests: python -m unittest server.tests.test_course_orchestrator -v</step>
    <step>Run all orchestration tests: python -m unittest discover server/tests -p "test_*orchestrator*.py" -v</step>
    <step>Verify no regressions: python -m unittest discover server/tests -v</step>
  </verification>

  <success-criteria>
    <criterion>All unit tests pass</criterion>
    <criterion>Async tests properly use IsolatedAsyncioTestCase</criterion>
    <criterion>Partial failure scenarios covered</criterion>
    <criterion>Context injection verified for all positions</criterion>
    <criterion>Integration test available for manual verification</criterion>
  </success-criteria>

  <output>
    On completion, create 03-02-SUMMARY.md documenting:
    - Test coverage summary
    - Async testing patterns used
    - Integration test instructions
    - Commit hash
  </output>
</plan>
