<plan>
  <metadata>
    <phase>02</phase>
    <plan-number>01</plan-number>
    <title>Instructor Library Integration</title>
    <status>pending</status>
  </metadata>

  <objective>
    Integrate the Instructor library with Vertex AI for structured output validation.
    This is the foundation for all AI agents.
  </objective>

  <context>
    <file path=".planning/BRIEF.md" />
    <file path="server/utils/vertex_client.py" />
    <file path="server/schemas/learning.py" />
    <file path="Implementing Retrieval-Based Learning Feature.md" section="2.3: Structured Output Enforcement" />
  </context>

  <prerequisites>
    <prerequisite>Phase 01 completed (database schemas exist)</prerequisite>
  </prerequisites>

  <tasks>
    <task id="1">
      <title>Install Instructor Library</title>
      <type>setup</type>
      <files>
        <file>server/requirements.txt</file>
      </files>
      <action>
        Add to requirements.txt:
        - instructor>=1.0.0
        - tenacity>=8.2.0
        
        Then run: pip install instructor tenacity
      </action>
      <verify>
        <command>python -c "import instructor; print(f'Instructor version: {instructor.__version__}')"</command>
      </verify>
      <done>false</done>
    </task>

    <task id="2">
      <title>Create Instructor Client Wrapper</title>
      <type>implement</type>
      <files>
        <file>server/utils/instructor_client.py</file>
      </files>
      <action>
        Create InstructorClient class that:
        
        - Defines MODEL_CONFIGS dict with settings for planner/generator/quizzer roles
        - Provides init() method that patches Vertex AI models with Instructor
        - Uses instructor.from_vertexai() with VERTEXAI_TOOLS mode
        - Implements create_structured() async method with:
          - Role-based model selection
          - Pydantic response_model validation
          - Tenacity retry decorator (3 attempts, exponential backoff)
          - Proper error handling and logging
        
        Model configs:
        - planner: gemini-1.5-pro, temp=0.3, max_tokens=4096
        - generator: gemini-1.5-flash, temp=0.7, max_tokens=2048
        - quizzer: gemini-1.5-flash, temp=0.2, max_tokens=1024
      </action>
      <verify>
        <command>python -c "from server.utils.instructor_client import instructor_client; print('InstructorClient imported')"</command>
      </verify>
      <done>false</done>
    </task>

    <task id="3">
      <title>Integrate Instructor Initialization</title>
      <type>implement</type>
      <files>
        <file>server/main.py</file>
      </files>
      <action>
        Update lifespan function to initialize Instructor after Vertex AI:
        
        1. Import instructor_client from server.utils.instructor_client
        2. After Vertex AI initialization block, add:
           - Try to call instructor_client.init()
           - Log success or warning appropriately
           - Handle exceptions without crashing app
      </action>
      <verify>
        <command>Start server and check logs for "Instructor clients initialized successfully"</command>
      </verify>
      <done>false</done>
    </task>

    <task id="4">
      <title>Create Base Agent Class</title>
      <type>implement</type>
      <files>
        <file>server/agents/__init__.py</file>
        <file>server/agents/base.py</file>
      </files>
      <action>
        Create BaseAgent abstract class with:
        
        - __init__(role: str): Store agent role
        - Abstract property system_prompt -> str
        - generate() async method that calls instructor_client.create_structured()
        - _build_system_prompt() for context injection
        - _format_context() for prompt augmentation
        
        Create __init__.py exporting BaseAgent
      </action>
      <verify>
        <command>python -c "from server.agents import BaseAgent; print('BaseAgent imported')"</command>
      </verify>
      <done>false</done>
    </task>
  </tasks>

  <verification>
    <step>Verify instructor installed: pip show instructor</step>
    <step>Verify tenacity installed: pip show tenacity</step>
    <step>Verify imports: python -c "from server.utils.instructor_client import instructor_client; from server.agents import BaseAgent"</step>
    <step>Start server and verify initialization logs</step>
  </verification>

  <success-criteria>
    <criterion>Instructor library installed and importable</criterion>
    <criterion>Tenacity library installed and importable</criterion>
    <criterion>InstructorClient initializes successfully</criterion>
    <criterion>BaseAgent class is importable</criterion>
    <criterion>Server starts without errors</criterion>
  </success-criteria>

  <output>
    On completion, create 02-01-SUMMARY.md documenting:
    - Dependencies installed
    - Files created
    - Initialization verification
    - Commit hash
  </output>
</plan>
