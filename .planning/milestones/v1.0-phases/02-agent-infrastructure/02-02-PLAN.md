<plan>
  <metadata>
    <phase>02</phase>
    <plan-number>02</plan-number>
    <title>Planner Agent Implementation</title>
    <status>pending</status>
  </metadata>

  <objective>
    Implement the Planner Agent that decomposes user queries into structured
    learning paths using the KLI (Knowledge-Learning-Instruction) framework.
  </objective>

  <context>
    <file path=".planning/phases/02-agent-infrastructure/02-01-PLAN.md" />
    <file path="server/agents/base.py" />
    <file path="server/schemas/learning.py" />
    <file path="Implementing Retrieval-Based Learning Feature.md" section="3.1: The Planner Agent" />
  </context>

  <prerequisites>
    <prerequisite>Plan 02-01 completed (Instructor integration)</prerequisite>
  </prerequisites>

  <tasks>
    <task id="1">
      <title>Implement Planner Agent</title>
      <type>implement</type>
      <files>
        <file>server/agents/planner.py</file>
      </files>
      <action>
        Create PlannerAgent class extending BaseAgent:
        
        System prompt must include:
        - Role definition as instructional designer
        - KLI framework explanation
        - Hierarchical decomposition guidelines
        - Requirement for 5-7 sub-concepts
        - Prerequisite ordering constraint
        - Context injection explanation (summary_for_context purpose)
        - Example decomposition for "Newtonian Laws"
        
        Implement:
        - __init__(): Call super with role="planner"
        - system_prompt property: Return PLANNER_SYSTEM_PROMPT
        - plan(query: str) -> CourseOutline: Generate curriculum
        
        Create singleton: planner_agent = PlannerAgent()
      </action>
      <verify>
        <command>python -c "from server.agents.planner import planner_agent; print('PlannerAgent imported')"</command>
      </verify>
      <done>false</done>
    </task>

    <task id="2">
      <title>Create Planner Agent Tests</title>
      <type>test</type>
      <files>
        <file>server/tests/test_planner_agent.py</file>
      </files>
      <action>
        Create unit tests (mock-based):
        
        TestPlannerAgent:
        - test_agent_role: Verify role is "planner"
        - test_system_prompt_contains_kli: Check KLI framework in prompt
        - test_system_prompt_contains_guidelines: Check key guidelines
        - test_plan_calls_generate: Mock instructor_client, verify call
        - test_topic_node_validation: Verify TopicNode schema
        - test_course_outline_validation: Verify CourseOutline schema
        
        TestPlannerPromptQuality:
        - test_prompt_defines_role: Check role definition present
        - test_prompt_has_output_requirements: Check requirements section
        - test_prompt_has_example: Check example decomposition
        - test_prompt_explains_context_injection: Check context injection docs
      </action>
      <verify>
        <command>python -m unittest server.tests.test_planner_agent -v</command>
      </verify>
      <done>false</done>
    </task>

    <task id="3">
      <title>Update Agents Package Exports</title>
      <type>implement</type>
      <files>
        <file>server/agents/__init__.py</file>
      </files>
      <action>
        Update __init__.py to export:
        - BaseAgent
        - PlannerAgent
        - planner_agent (singleton instance)
      </action>
      <verify>
        <command>python -c "from server.agents import planner_agent; print('planner_agent singleton imported')"</command>
      </verify>
      <done>false</done>
    </task>
  </tasks>

  <verification>
    <step>Import test: python -c "from server.agents import planner_agent"</step>
    <step>Run unit tests: python -m unittest server.tests.test_planner_agent -v</step>
    <step>Verify prompt quality via manual inspection</step>
  </verification>

  <success-criteria>
    <criterion>PlannerAgent class implemented</criterion>
    <criterion>System prompt includes KLI framework</criterion>
    <criterion>All unit tests pass</criterion>
    <criterion>Agent is exported from agents package</criterion>
  </success-criteria>

  <output>
    On completion, create 02-02-SUMMARY.md documenting:
    - Agent implementation details
    - Prompt engineering decisions
    - Test results
    - Commit hash
  </output>
</plan>
