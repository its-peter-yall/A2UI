<plan>
  <metadata>
    <phase>02</phase>
    <plan-number>03</plan-number>
    <title>Generator and Quizzer Agents</title>
    <status>pending</status>
  </metadata>

  <objective>
    Implement the Generator Agent (creates explanations) and Quizzer Agent
    (generates quizzes) with context injection for narrative coherence.
  </objective>

  <context>
    <file path=".planning/phases/02-agent-infrastructure/02-02-PLAN.md" />
    <file path="server/agents/base.py" />
    <file path="server/agents/planner.py" />
    <file path="server/schemas/learning.py" />
    <file path="Implementing Retrieval-Based Learning Feature.md" section="3.2-3.3: Generator and Quizzer" />
  </context>

  <prerequisites>
    <prerequisite>Plan 02-02 completed (Planner Agent)</prerequisite>
  </prerequisites>

  <tasks>
    <task id="1">
      <title>Implement Generator Agent</title>
      <type>implement</type>
      <files>
        <file>server/agents/generator.py</file>
      </files>
      <action>
        Create GeneratorAgent class extending BaseAgent:
        
        System prompt must include:
        - Role as educational content creator
        - Concise, targeted explanation guidelines
        - Context injection instructions (use prev/next summaries)
        - Markdown formatting requirements
        - Engagement and enthusiasm guidelines
        - 2-3 minute reading time target
        
        Implement:
        - __init__(): Call super with role="generator"
        - system_prompt property
        - generate_explanation(topic: TopicNode, prev_summary: str, next_summary: str) -> str
          - Inject context from adjacent topics
          - Return markdown content
        
        Create Pydantic model for response:
        - GeneratedContent(content_markdown: str, key_takeaways: List[str])
        
        Create singleton: generator_agent = GeneratorAgent()
      </action>
      <verify>
        <command>python -c "from server.agents.generator import generator_agent; print('GeneratorAgent imported')"</command>
      </verify>
      <done>false</done>
    </task>

    <task id="2">
      <title>Implement Quizzer Agent</title>
      <type>implement</type>
      <files>
        <file>server/agents/quizzer.py</file>
      </files>
      <action>
        Create QuizzerAgent class extending BaseAgent:
        
        System prompt must include:
        - Role as assessment designer
        - Retrieval-based learning principles
        - Distractor generation guidelines (target misconceptions)
        - Strict JSON output requirements
        - Difficulty calibration guidelines
        - Explanation requirements for each option
        
        Implement:
        - __init__(): Call super with role="quizzer"
        - system_prompt property
        - generate_quiz(topic: TopicNode, content: str) -> QuizCard
          - Use topic and generated content
          - Return validated QuizCard with options
        
        Create singleton: quizzer_agent = QuizzerAgent()
      </action>
      <verify>
        <command>python -c "from server.agents.quizzer import quizzer_agent; print('QuizzerAgent imported')"</command>
      </verify>
      <done>false</done>
    </task>

    <task id="3">
      <title>Create Generator and Quizzer Tests</title>
      <type>test</type>
      <files>
        <file>server/tests/test_generator_agent.py</file>
        <file>server/tests/test_quizzer_agent.py</file>
      </files>
      <action>
        Create mock-based unit tests for both agents:
        
        TestGeneratorAgent:
        - test_agent_role: Verify role is "generator"
        - test_system_prompt_structure: Check key guidelines
        - test_context_injection: Verify prev/next context used
        - test_generate_explanation_returns_content: Mock test
        
        TestQuizzerAgent:
        - test_agent_role: Verify role is "quizzer"
        - test_system_prompt_structure: Check assessment guidelines
        - test_distractor_guidelines: Check misconception targeting
        - test_generate_quiz_returns_valid_card: Mock test
        - test_quiz_card_has_explanations: Verify all options explained
      </action>
      <verify>
        <command>python -m unittest server.tests.test_generator_agent server.tests.test_quizzer_agent -v</command>
      </verify>
      <done>false</done>
    </task>

    <task id="4">
      <title>Update Agents Package Exports</title>
      <type>implement</type>
      <files>
        <file>server/agents/__init__.py</file>
      </files>
      <action>
        Update __init__.py to export:
        - BaseAgent
        - PlannerAgent, planner_agent
        - GeneratorAgent, generator_agent
        - QuizzerAgent, quizzer_agent
      </action>
      <verify>
        <command>python -c "from server.agents import planner_agent, generator_agent, quizzer_agent; print('All agents imported')"</command>
      </verify>
      <done>false</done>
    </task>
  </tasks>

  <verification>
    <step>Import all agents: python -c "from server.agents import planner_agent, generator_agent, quizzer_agent"</step>
    <step>Run generator tests: python -m unittest server.tests.test_generator_agent -v</step>
    <step>Run quizzer tests: python -m unittest server.tests.test_quizzer_agent -v</step>
    <step>Run all agent tests: python -m unittest discover server/tests -p "test_*agent*.py" -v</step>
  </verification>

  <success-criteria>
    <criterion>GeneratorAgent implemented with context injection</criterion>
    <criterion>QuizzerAgent implemented with distractor guidelines</criterion>
    <criterion>All unit tests pass</criterion>
    <criterion>All agents exported from package</criterion>
  </success-criteria>

  <output>
    On completion, create 02-03-SUMMARY.md documenting:
    - Agent implementations
    - Context injection approach
    - Prompt engineering for distractors
    - Test results
    - Commit hash
  </output>
</plan>
