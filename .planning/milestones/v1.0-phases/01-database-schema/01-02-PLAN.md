<plan>
  <metadata>
    <phase>01</phase>
    <plan-number>02</plan-number>
    <title>Database Schema Tests</title>
    <status>pending</status>
  </metadata>

  <objective>
    Create unit tests for the learning database schemas and persistence layer
    to ensure data integrity and correct behavior.
  </objective>

  <context>
    <file path=".planning/phases/01-database-schema/01-01-PLAN.md" />
    <file path="server/database/learning_persistence.py" />
    <file path="server/schemas/learning.py" />
    <file path="server/tests/test_sessions.py" note="for test patterns" />
  </context>

  <tasks>
    <task id="1">
      <title>Create Schema Validation Tests</title>
      <type>test</type>
      <files>
        <file>server/tests/test_learning_schemas.py</file>
      </files>
      <action>
        Create tests for Pydantic schema validation:
        
        TestNodeStatus:
        - test_status_values: Verify enum values match expected strings
        
        TestQuizSchemas:
        - test_quiz_option_valid: Valid QuizOption creation
        - test_quiz_card_valid: Valid QuizCard with options
        - test_quiz_card_requires_min_options: Validation error on single option
        
        TestPlannerSchemas:
        - test_topic_node_valid: Valid TopicNode creation
        - test_course_outline_valid: Valid CourseOutline with 5 topics
        - test_course_outline_min_topics: Validation error on 2 topics
        
        TestSessionSchemas:
        - test_learning_session_create: Valid LearningSessionCreate
        - test_concept_node_create: Valid ConceptNodeCreate
        
        TestQuizSubmission:
        - test_submission_valid: Valid QuizSubmission
      </action>
      <verify>
        <command>python -m unittest server.tests.test_learning_schemas -v</command>
      </verify>
      <done>false</done>
    </task>

    <task id="2">
      <title>Create Persistence Layer Tests</title>
      <type>test</type>
      <files>
        <file>server/tests/test_learning_persistence.py</file>
      </files>
      <action>
        Create tests for LearningManager database operations:
        
        TestLearningManager (with temp database per test):
        - test_create_learning_session: Create and verify session fields
        - test_get_learning_session: Retrieve created session
        - test_get_nonexistent_session: Returns None for missing ID
        - test_create_concept_node: Create node with all fields
        - test_create_node_with_quiz: Create node with quiz payload
        - test_get_session_nodes: Get ordered nodes for session
        - test_update_node_status: Update status and verify
        - test_get_next_node: Get next node by sequence index
        - test_get_next_node_none: Returns None when no next node
        - test_get_quiz_for_node: Retrieve quiz JSON payload
        - test_cascade_delete: Verify FK cascade on session delete
      </action>
      <verify>
        <command>python -m unittest server.tests.test_learning_persistence -v</command>
      </verify>
      <done>false</done>
    </task>
  </tasks>

  <verification>
    <step>Run schema tests: python -m unittest server.tests.test_learning_schemas -v</step>
    <step>Run persistence tests: python -m unittest server.tests.test_learning_persistence -v</step>
    <step>Run all tests: python -m unittest discover server/tests -v</step>
  </verification>

  <success-criteria>
    <criterion>All schema validation tests pass</criterion>
    <criterion>All persistence layer tests pass</criterion>
    <criterion>Cascade delete works correctly</criterion>
    <criterion>No regressions in existing tests</criterion>
  </success-criteria>

  <output>
    On completion, create 01-02-SUMMARY.md documenting:
    - Test coverage summary
    - All tests passing
    - Any edge cases discovered
    - Commit hash
  </output>
</plan>
